{
    name: release,
    on:
        {
            push: { branches: ["*"] },
            pull_request: { types: [ready_for_review, converted_to_draft] },
        },
    permissions:
        {
            id-token: write,
            contents: write,
            packages: write,
            actions: write,
            deployments: write,
            pull-requests: write,
            issues: write,
            statuses: write,
            checks: write,
            repository-projects: write,
        },
    env: { GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}" },
    jobs:
        {
            override:
                {
                    runs-on: ubuntu-latest,
                    steps:
                        [
                            { uses: actions/checkout@v3 },
                            { id: setup-go, uses: actions/setup-go@v4 },
                            { id: download-go-deps, run: go mod download },
                            {
                                id: build-linux-amd64,
                                run: "${{ github.workspace }}/build.sh linux amd64 ~/.buildrc-cache/buildrc-override/linux-amd64",
                            },
                            {
                                id: setup-buildrc,
                                uses: nuggxyz/actions/setup-buildrc@v0.0.36,
                            },
                        ],
                },
            setup:
                {
                    runs-on: ubuntu-latest,
                    needs: [override],
                    if: "${{ github.event_name == 'push' }}",
                    outputs:
                        {
                            BUILDRC_LOAD_PACKAGE_NAMES_ARRAY: "${{ env.BUILDRC_LOAD_PACKAGE_NAMES_ARRAY }}",
                        },
                    steps:
                        [
                            { uses: actions/checkout@v3 },
                            {
                                id: setup-buildrc,
                                uses: nuggxyz/actions/setup-buildrc@v0.0.36,
                            },
                            { id: setup-release, run: "buildrc release setup" },
                        ],
                },
            build:
                {
                    runs-on: ubuntu-latest,
                    needs: [setup],
                    if: "${{ github.event_name == 'push' }}",
                    strategy:
                        {
                            matrix:
                                {
                                    package: "${{ fromJson(needs.setup.outputs.BUILDRC_LOAD_PACKAGE_NAMES_ARRAY) }}",
                                },
                        },
                    steps:
                        [
                            { uses: actions/checkout@v3 },
                            {
                                id: setup-buildrc,
                                uses: nuggxyz/actions/setup-buildrc@v0.0.36,
                            },
                            {
                                id: load-package,
                                run: "buildrc package ${{ matrix.package }}",
                            },
                            {
                                id: setup-go,
                                if: "${{env.BUILDRC_PACKAGE_USES_GO == '1'}}",
                                uses: actions/setup-go@v4,
                            },

                            { id: build, run: "buildrc release build" },
                        ],
                },
            finalize:
                {
                    runs-on: ubuntu-latest,
                    needs: [build],
                    steps:
                        [
                            { uses: actions/checkout@v3 },
                            {
                                id: setup-buildrc,
                                uses: nuggxyz/actions/setup-buildrc@0.0.9,
                            },
                            {
                                id: pre-release,
                                run: "buildrc release finalize",
                            },
                        ],
                },
        },
}
